# Voice Assistant Dependencies - Pipecat Framework
# Simplified to avoid dependency conflicts

# Core Pipecat framework (install base first)
pipecat-ai==0.0.52

# LLM Providers - install individually to avoid conflicts
groq>=0.9.0

# OpenAI SDK (required by Pipecat's internal aggregators even when using Groq)
openai>=1.0.0

# TTS Provider SDK
# Deepgram SDK v3+ (pinned for stability)
deepgram-sdk==3.4.0

# Async HTTP (required by Pipecat)
aiohttp>=3.9.0

# Audio processing
numpy
scipy
pyaudio>=0.2.14
